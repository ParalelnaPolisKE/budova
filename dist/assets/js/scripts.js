const a = 5

const obj = {
  lol: 14
}

function test() {
  const vyslednahodnota = a + obj.lol
  return "hello world" + vyslednahodnota
}

document.querySelectorAll('.cocoen').forEach(function(element){
  new Cocoen(element);
});
